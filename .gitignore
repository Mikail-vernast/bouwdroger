# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Lokale Supabase-CLI-state, hoort niet in een publieke repo
supabase/.temp/

# Snapshots en consolelogs van de Playwright MCP. Ze staan vol met de URL's die
# tijdens het testen bezocht zijn, en op /verhuur/saldo en /verhuur/verlengen
# staat het Stripe-sessie-ID ín die URL — het enige dat die pagina's afschermt.
# Vandaar hier: deze repo is publiek en één `git add -A` is genoeg.
.playwright-mcp/

# tsc --noEmit laat deze achter; ze horen bij de machine, niet bij de repo.
*.tsbuildinfo

# Python-bytecode van de hooks in .claude/
__pycache__/
